package Util;

import javafx.scene.paint.Color;

public class Parser {

    /**
     * ToDo
     * parses the text to extract a color from it in the format "red,green,blue" with red, green and blue bound [0,255]
     * @param text
     * @return
     */
    public static Color parseColor(String text) {
        return new Color(0, 0 ,0, 0);
    }

}
